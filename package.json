{
  "name": "fastshop",
  "version": "1.0.0",
  "description": "FastShop - Microservices E-Commerce Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd services/product-service && npm install && cd ../order-service && npm install && cd ../../gateway && npm install && cd ../frontend && npm install",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": ["microservices", "react", "express", "docker"],
  "author": "",
  "license": "ISC"
}
